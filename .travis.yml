language: node_js
sudo: required

node_js:
 - "8"
 - "10"

branches:
  only:
    - master

services:
  - mongodb
  - docker

install:
 - npm install

before_install:
 - test/docker-compose up -d
 - npm update -q

# before_script:
 # - npm run lint

after_script: 
  - npm run test:coveralls
